module.exports = {
    webpack: {
      configure: (webpackConfig, { env, paths }) => {
        // Modifique aqui as configurações do Webpack conforme necessário
        return webpackConfig;
      },
    },
  };
